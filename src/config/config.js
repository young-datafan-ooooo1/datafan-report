// 自定义配置，参考 ./default/setting.config.js，需要自定义的属性在这里配置即可
module.exports = {
  theme: {
    color: '#FF794A',
    mode: 'dark'
  },
  multiPage: false, // 是否多标签
  animate: {
    name: 'fade',
    direction: 'default'
  }
}
